# Enable auto-registration of JUnit 5 extensions from the classpath
# This allows FailureAnalyzerExtension to activate without @ExtendWith on every class
junit.jupiter.extensions.autodetection.enabled=true
